"use strict";

var wowDB = function(db, wowApi) {
  this.getItem = function(objectID, callback) {

  };
};

wowDB.prototype.getItem = function(itemID, callback) {
  this.getItem(itemID, callback);
};

module.exports = wowDB;
